spring:
  application:
    name: lodge_hotel_restapi
  profiles:
    active: test
  datasource:
    initialization-mode: never
  sql:
    init:
      mode: never

server:
  port: 8090

keyStore:
  path: keystore.jks
  password: password
  alias: auth-server